import ScrollReveal from 'scrollreveal'

const sr = {
  func: ScrollReveal(),
  config: {
    reset: false,
    duration: 400,
    easing: 'cubic-bezier(.694,0,.335,1)',
    scale: 1,
    viewFactor: 0.3,
  }
}

export default sr