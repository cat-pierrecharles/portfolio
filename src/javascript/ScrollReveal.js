import ScrollReveal from 'scrollreveal'

const sr = {
  func: ScrollReveal(),
  config: {
    reset: false,
    origin: 'bottom',
    duration: 200,
    easing: 'ease-in',
    scale: 1,
    viewFactor: 0.3,
  }
}

export default sr